$schema: ../../schemas/aici.schema.json
version: 1
tests:
  - name: refund-policy-support-response
    mockOutputFile: output.json
    expect:
      contains:
        - approved
      jsonSchema: schema.json
      maxLatencyMs: 1000
